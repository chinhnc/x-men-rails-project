<nav class="navbar navbar-inverse navbar-fixed-top" data-no-turbolink>
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed"
        data-toggle="collapse" data-target="#navbar"
        aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_url, class: "logo" do %>
        <%= image_tag "logo.ico", title: t("project_name") %>
      <% end %>
      <%= link_to t("project_name"), root_path, class: "navbar-brand" %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <% if current_user.is_admin? %>
            <li><%= link_to t("layout.header.categories"), admin_categories_path %></li>
            <li><%= link_to t("layout.header.anime"), admin_animes_path %></li>
          <% else %>
            <li><%= link_to t("layout.header.anime"), animes_path %></li>
          <% end %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"
              role="button" aria-haspopup="true" aria-expanded="false">
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t("layout.header.profile"), current_user %></li>
              <li><%= link_to t("layout.header.logout"),
                destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t("layout.header.signup"), new_user_registration_path %></li>
          <li><%= link_to t("layout.header.login"), new_user_session_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
